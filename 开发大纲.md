# RVC AI跟唱客户端（对标 SoundTrail 声迹）分层开发架构需求总纲

> 文档格式：Markdown  
> 技术栈：Python 3.12 + PyQt6 + 本仓库 RVC 内核 + MSST(pymss) + RMVPE + sounddevice + OSC/MTC + pywin32 + Nuitka  
> 运行平台：Windows 10/11  
> 音频固定链路：麦克风 → Voicemeeter 虚拟声卡 → 本软件 RVC 实时转换 → Studio One DAW 混音机架  
> 开发工具：Cursor 分段自动化开发、单模块独立测试、迭代验收  
> 交付产物：Windows 独立桌面客户端 | 内置远程崩溃日志上报 + 增量自动更新系统

## 审查修订记录（2026-08-04）

| 类别 | 原稿问题 | 修订说明 |
| --- | --- | --- |
| 产品定位 | 未说明与 SoundTrail 的差异 | 补充「对标范围」：离线做歌可对齐；实时跟唱本方案走**麦克风实时 RVC**，SoundTrail 走**预渲染 AI 人声 + 自研修音** |
| 离线链路 | 缺去混响阶段；`other` 误当和声 | 对齐声迹日志：分离 → 去混响 →（可选）和声提取 → RVC → 四轨导出 |
| 任务顺序 | 实时标最高优但 UI 先全量开发；调度层放最后 | 调整为：**调度骨架 → 离线链路（复用现有代码）→ 实时链路 → 歌词/运维 → 全量 UI 完善** |
| 模块依赖 | MSST 依赖 RVC 推理；离线 RVC 依赖音频 IO | 分离 MSST/RVC 离线/RVC 实时三条依赖线 |
| 采样率 | 全局写死 48000Hz | MSST 固定 44100Hz，RVC 按模型采样率；统一在 IO 层做重采样 |
| 实时缓冲 | 50ms 环形缓冲 | 改为 100~500ms 可配置，参考 `realtime_gui.py` / `RVCRealtimeVST` |
| 歌词 F0 约束 | 写「RVC 接收字级 F0 区间」 | 标注为**自研扩展**（标准 RVC 不支持），分 Phase 2 可选实现 |
| 代码复用 | 写「剥离 Gradio 重写」 | 明确复用 `tools/song_cover.py`、`tools/pymss_webui.py`、`tools/pymss/workflow.py`、`infer/rtrvc.py`、`RVCRealtimeVST/` |
| 打包 | 仅 Nuitka | 补充风险：CUDA+Torch 打包困难，备选嵌入式 Python 目录（与声迹 `msst-reflow` 同类方案） |
| 模型格式 | 支持 `.model` | `.model` 为声迹私有封装，首版仅支持 RVC 标准 `.pth` + `.index` |

---

## 文档使用说明（给 Cursor）

1. 本文档为项目全局唯一总纲，所有开发逻辑、分层规则、功能需求均以此为准，不可偏离；
2. 严格按照【分段任务 0~7】顺序依次开发，单个模块独立可运行测试、验收合格后再进入下一模块；
3. 全程遵守四层分层解耦规范：**控制消息**走调度层，**音频 PCM 热路径**可在内核层直连（见下文例外）；
4. 优先复用本仓库已有实现，禁止重复造轮子；新增代码保持最小 diff。

---

# 一、整体四层分层架构（强制全局标准）

自上而下分层，每层职责完全隔离，单模块可独立开发、单元测试，互不阻塞迭代。

## 1. UI 表现层（PyQt6 桌面界面）

- 仅负责窗口渲染、控件交互、数据可视化展示；
- 无 AI 推理、音频 IO 阻塞、大文件读写等耗时业务逻辑；
- 统一 Qt 信号槽异步通信，不使用全局变量传递状态数据。

## 2. 业务调度核心层（全局单例调度总线）

- 系统统一**控制消息**中转中心，串联 UI、音频、RVC 推理、歌词、更新、日志全部模块；
- 统一管控所有子线程生命周期，程序退出安全释放 CUDA 显存、音频流资源；
- 全局唯一时间基准分发：Studio One OSC/MTC 播放头时间戳 `T`；
- 统一中转异常、进度、状态信号，统一分发至 UI 与日志上报模块。

> **热路径例外（低延迟强制）**  
> 麦克风 PCM → 环形缓冲 → RVC 实时推理 → 输出缓冲，这条链路可在「底层能力内核层」内直连，不经调度层转发音频数据；调度层只负责启停、参数变更、模型热切换指令。

## 3. 底层能力内核层（可独立抽离 Python 库，无界面可单独测试）

拆分为 5 个完全独立子模块：

1. **音频 IO 硬件模块**：声卡/Voicemeeter/ASIO 枚举、环形音频缓冲、流式采集输出
2. **RVC 推理内核模块**：实时流式转换、离线批量翻唱、模型无断音热切换、RMVPE 音高提取
3. **MSST 人声分离内核模块**：原唱人声/伴奏/和声/去混响，两套业务参数预设
4. **歌词同步时钟模块**：LRC 解析、OSC/MTC 读取 DAW 时间、悬浮歌词渲染、（可选）音高区间约束下发
5. **系统运维工具模块**：硬件检测、分级本地日志、远程崩溃上报、客户端自动更新

## 4. 底层依赖基础层（统一环境管理）

FFmpeg、PyTorch(CUDA)、sounddevice、LoopMIDI、OSC 通信库、pywin32、Nuitka 打包工具

### 本仓库可直接复用的现有资产

| 模块 | 路径 | 用途 |
| --- | --- | --- |
| 一键翻唱离线链路 | `tools/song_cover.py` | 分离 → RVC → 混音，可扩展为多阶段 |
| MSST WebUI 封装 | `tools/pymss_webui.py` | 分离/去混响/提主旋律模型与进度事件 |
| MSST 多步工作流 | `tools/pymss/workflow.py` | YAML 编排分离→去混响→和声 |
| 实时 RVC 核心 | `infer/rtrvc.py` | 流式推理、formant、RMVPE |
| 实时 GUI 参考 | `realtime_gui.py` | 块大小、formant、设备参数 |
| 实时 VST 参考 | `RVCRealtimeVST/worker/rvc_worker.py` | 共享内存 IPC、无断音切换 |

---

# 二、全局统一强制规范（所有模块必须遵守）

1. 所有耗时任务（AI 推理、模型加载、音频分块、网络更新）全部放入独立子线程，禁止阻塞 UI 主线程；
2. **控制状态**（启停、进度、异常、参数）仅通过异步信号传递；音频 PCM 热路径允许内核层环形缓冲直连；
3. 全局唯一时间基准：Studio One 输出播放头秒级时间戳 `T`，歌词渲染依赖 `T`；RVC 不参与时间计算；
4. 音频隔离规则：**伴奏全程不进入 RVC 推理**；实时模式下仅原始麦克风人声送入 AI 转换；离线模式下仅 MSST 分离/去混响后的干人声送入 RVC；
5. 采样率策略：MSST 内部固定 **44100Hz**（见 `tools/pymss_webui.py`）；RVC 按模型 `sampling_rate`（常见 40000/48000）；音频 IO 默认 **48000Hz**，边界处显式重采样，禁止隐式假设全链路同采样率；
6. 用户配置持久化：声卡参数、RVC 推理参数、界面布局、更新地址、热键全部存入本地 JSON，开机自动加载；
7. 日志分级：`DEBUG / INFO / WARN / ERROR`；本地滚动分文件存储，程序异常自动打包日志远程上报。

---

# 三、对标 SoundTrail 的范围说明

| 能力 | SoundTrail 实现 | 本客户端目标 | 首版是否实现 |
| --- | --- | --- | --- |
| 离线做歌 | MSST 多阶段 + RVC + 四轨导出 | 对齐（见链路 2） | ✅ 必做 |
| AI 唱歌 | 预渲染 AI 人声轨播放 | 离线生成 AI 人声 + 播放 | ✅ 必做 |
| AI 跟唱 | 预渲染 AI 人声 + **自研实时修音** | **麦克风实时 RVC** + Studio One 修音（或后续自研修音） | ⚠️ 路径不同，效果需实测 |
| 四轨 | 伴奏/人声/原唱/和声 | 同上 | ✅ 必做 |
| 歌词悬浮 | LRC + 时间同步 | OSC/MTC + 悬浮窗 | ✅ 必做 |
| 字级 F0 约束 RVC | 可能有 | 标准 RVC **不支持**，需自研后处理 | ⏳ Phase 2 可选 |
| `.model` 模型包 | 私有格式 | 首版仅 `.pth` + `.index` | ⏳ 后续 |

---

# 四、四大核心业务链路完整需求

## 链路 1：实时 AI 变声/跟唱直播链路（第三阶段集成，依赖离线资产验证）

### 硬件信号流向

物理麦克风 → Voicemeeter 硬件输入通道 → 软件音频 IO 线程采集人声 → RVC 流式实时推理（RMVPE + 音色转换 + formant） → 转换后人声回传 Voicemeeter VAIO → Studio One 机架 EQ/压缩/混响/自动修音混音 → Voicemeeter 分流耳机监听 + OBS 直播推流

> **说明**：Studio One 侧「自动修音」替代了 SoundTrail 内置修音引擎；若需更接近声迹「AI 跟唱」，后续需自研 pitch correction 模块（Phase 2）。

### 配套联动逻辑

1. 后台 OSC/MTC 线程持续读取 Studio One 播放头全局时间 `T`（歌词用，不驱动 RVC 音频时钟）；
2. LRC 模块根据 `T` 实时高亮歌词；字级 F0 区间约束为**可选扩展**，首版可只做歌词高亮；
3. 多模型后台预缓存，无断音热切换：仅替换推理权重/索引，音频流不中断（参考 `RVCRealtimeVST` 切换逻辑）；
4. 全局自定义热键：启停 AI、切换音色预设、伴奏播放同步；
5. 系统托盘后台常驻，窗口最小化不销毁音频与推理线程；
6. 实时缓冲：**100~500ms** 可配置块大小 + 环形缓冲，禁止固定 50ms（延迟与稳定性不足）。

## 链路 2：离线 AI 翻唱制作链路（第二阶段开发，复刻声迹「制作歌曲」，**优先开发**）

### 业务流程（对齐声迹日志）

原唱音频导入 → FFmpeg 解码 → **Stage1** MSST 人声/伴奏分离 → **Stage2** MSST 人声去混响 → **Stage3（可选）** MSST 提主旋律/和声分离 → RVC 批量转换干人声 → 导出四轨 + 可选 FFmpeg 合并成品

```text
输入整曲
  → vocals.wav          （原唱人声，保留）
  → instrumental.wav    （伴奏）
  → vocals_noreverb.wav （去混响干声，RVC 输入）
  → harmony.wav         （和声，karaoke 模型 off_vocal/other）
  → converted_vocal.wav （RVC 输出 AI 人声）
  → cover.wav           （可选：AI 人声 + 伴奏混音）
```

### 两套分离预设（对标普通/强力做歌）

| 预设 | Stage1 分离 | Stage2 去混响 | Stage3 和声 | 说明 |
| --- | --- | --- | --- | --- |
| 普通做歌 | `去伴奏` bs_roformer_368 | `去混响` less-aggressive | 跳过 | 速度快，适合直播素材 |
| 强力做歌 | `去伴奏（激进）` bs_roformer_317 | `去混响（激进）` | `提主旋律` karaoke | 质量优先，输出四轨 |

> **纠正**：Stage1 的 `other/instrumental` 是「非人声剩余轨」，**不是和声**；和声需 Stage3 karaoke 模型单独提取。

### 配套功能

1. 音频分块处理，实时进度日志，支持中途终止任务（复用 `pymss_separate` 事件回调）；
2. RVC 推理前 `_release_vc_gpu` / 分离后 `_restore_vc_gpu`（见 `tools/song_cover.py`，防 OOM）；
3. 自动识别显卡算力，算力不足自动降级 CPU 推理并弹窗提示；
4. 工程集合保存，批量存储多首歌曲参数，可重复加载复用；
5. 首版在 `tools/song_cover.py` 上扩展，不另起炉灶。

## 链路 3：歌词同步联动子系统（第四阶段开发，依赖调度层时间基准）

独立后台线程运行，不占用 UI、AI 推理资源：

1. LRC 拖拽导入、时间轴校准，结构化存储：`[起始秒数, 结束秒数, 歌词文本]`；F0 区间字段为可选扩展；
2. 双同步方案：**优先 Studio One OSC**（PreSonus OSC 面/自定义端口），备选 LoopMIDI 接收 MTC；
3. 独立 Qt 悬浮歌词窗口：无边框、置顶、透明、可拖拽、自定义字体/颜色/大小，适配 OBS 采集；
4. 首版仅推送歌词文本 + 高亮；F0 约束下发至 RVC 列为 Phase 2；
5. 歌词配置本地缓存，重启自动加载。

## 链路 4：软件运维子系统（与任务 1 并行，发布部署必备）

### 4.1 远程 Bug/异常上报

1. 全局捕获程序崩溃、CUDA 报错、声卡断开、IO 异常，自动抓取完整堆栈；
2. 本地日志自动打包，HTTP POST 上传远程日志服务；
3. 支持用户手动一键导出日志压缩包线下排错；
4. 上报携带环境信息：软件版本、Windows 版本、显卡型号、CUDA 算力、音频设备参数。

### 4.2 客户端自动更新

1. 启动后台线程请求远程 `version.json` 版本接口，对比本地版本；
2. 双更新策略：小版本增量补丁、大版本完整客户端包；
3. MD5 文件完整性校验，下载失败自动重试；更新完成弹窗提示重启；
4. 更新失败自动回滚旧版本，保证客户端不损坏；
5. 远程可配置：更新地址、强制更新开关、版本公告文本。

---

# 五、分模块开发任务清单（Cursor 分段执行，独立测试验收）

> **推荐开发顺序**：0 → 2 → 5 → 4-离线 → 1(骨架) → 3 → 4-实时 → 6 → 7 全量集成

## 分段任务 0：调度层最小骨架 + 配置/日志基座（最先开发）

### 开发需求

1. 调度器单例：线程注册/启停/退出钩子、信号中转（不含业务逻辑）；
2. JSON 配置读写、日志分级类；
3. 模块间信号协议定义（Typed dataclass 或 Enum，避免字符串魔法值）。

### 验收标准

独立脚本可启动调度器、写入日志、读写配置、模拟信号转发；无 UI、无音频。

---

## 分段任务 1：系统运维工具模块（可与任务 0 并行）

### 开发需求

1. 分级日志类：自动滚动分割、单文件大小限制、自定义存储路径；
2. 全局异常捕获装饰器：捕获 Python、CUDA、音频硬件异常；
3. 远程日志上报 HTTP 客户端，自动携带设备环境信息；
4. 自动更新工具类：版本请求、MD5 校验、增量下载、更新替换、版本回滚；
5. 硬件采集工具：读取 Windows、显卡、CUDA、全量音频设备列表。

### 验收标准

独立脚本可测试日志写入、硬件读取、版本校验、模拟异常自动打包日志；更新下载、校验、回滚流程完整。

---

## 分段任务 2：PyQt6 UI 表现层（骨架优先，细节后补）

### 开发需求

1. 主窗口对标 SoundTrail 布局：
   - 左侧：文件资源栏（伴奏/人声/LRC/模型管理）
   - 中部：标签页（实时跟唱、离线制作、模型管理、系统设置）
   - 右侧：参数面板（RVC 高级参数、MSST 参数、音频设备下拉框）
   - 底部：实时日志窗口、GPU 硬件监控状态栏
2. 独立悬浮歌词子窗口：无边框、置顶、透明度、拖拽、样式自定义；
3. 系统托盘完整菜单：启停 AI、打开主界面、退出、手动检查更新；
4. 设置页面：音频硬件、OSC/MTC 参数、更新地址、日志路径、全局热键；
5. 模型管理面板：批量导入 `.pth` + `.index`、删除、分组、后台预缓存；
6. UI 全部通过信号槽与调度层解耦，界面无业务计算、音频逻辑；
7. 窗口布局自动保存 JSON，开机还原。

### 验收标准

单独启动脚本完整渲染界面，所有控件交互正常；悬浮窗口、托盘功能可用；未接入音频仅友好提示，无崩溃报错。

---

## 分段任务 3：音频硬件 IO 调度模块（依赖任务 0，独立声卡测试）

### 开发需求

1. 封装 sounddevice，枚举 WDM/WASAPI 设备，自动识别 Voicemeeter/VB-Cable 虚拟声卡并标记；
2. 音频流管理类：创建/启停/销毁，内置 **100~500ms 可配置**环形缓冲池；
3. 音频 IO 默认：48000Hz、float32（内部计算）/ int16（设备输出可选），缓冲块可调；
4. Voicemeeter 通道专用识别逻辑，区分硬件输入/VAIO 人声/AUX 伴奏；
5. 音频容错：热插拔自动重连、爆音平滑；**不承诺关闭系统级独占模式**（取决于驱动，仅检测并提示）；
6. 独立子线程采集音频，PCM 写入内核环形缓冲；控制信号经调度层。

> **ASIO**：首版可不做 ASIO 原生支持，Voicemeeter 已可桥接 ASIO 设备；ASIO 直连列为后期扩展。

### 验收标准

独立脚本可枚举全部声卡；稳定创建输入输出流，低延迟无持续爆音；插拔声卡正常捕获异常提示。

---

## 分段任务 4：RVC 推理内核模块（拆为 4A 离线 / 4B 实时）

基于本仓库 `infer/rtrvc.py`、`infer/vc/` 封装纯 Python 调用类，**禁止重写 Gradio 已有逻辑**。

### 4A 离线批量推理（依赖任务 5 产出干声，**不依赖**任务 3 音频 IO）

1. 扩展 `tools/song_cover.py`：多阶段 MSST + 四轨导出 + 可选混音；
2. 分块转换，对接 FFmpeg 合并/导出；
3. 参数映射：音调偏移、formant（声音粗细）、Index 比例、protect、最小/最大 F0、说话人 ID、RMVPE/FCPE；
4. 推理进度、GPU 占用异步推送 UI 日志。

### 4B 实时流式推理（依赖任务 3，参考 `RVCRealtimeVST` + `realtime_gui.py`）

1. 多模型后台预加载；
2. 环形缓冲平滑切换，无断音热切换；
3. RMVPE F0、HuBERT 特征、RVC v2 完整管线；
4. 硬件自动调度：检测 CUDA，无 N 卡切 CPU，低算力显卡弹窗提示；
5. Phase 2 可选：外部歌词 F0 区间后处理（非 RVC 原生能力）。

### 验收标准

4A：独立脚本完成离线四轨导出 + 成品混音；4B：实时流稳定输出 AI 人声，切换音色无断音。

---

## 分段任务 5：MSST 人声分离内核模块（依赖 FFmpeg + 任务 0，**不依赖 RVC**）

### 开发需求

1. 封装 `tools/pymss_webui.py` / `tools/pymss/workflow.py`，实现「普通做歌/强力做歌」两套预设（见链路 2 表格）；
2. FFmpeg 解码全格式音频，输出标准 WAV（44100Hz）；
3. 大音频分块处理，限制内存，实时推送进度信号（复用 `on_separate_event`）；
4. 支持中途终止，自动清理临时缓存；
5. 分离完成推送文件路径信号至调度层。

### 验收标准

导入原唱自动完成 Stage1+2（强力含 Stage3）；两套模式输出文件结构不同；中断任务无残留缓存文件。

---

## 分段任务 6：歌词同步 & Studio One 时间同步模块（依赖任务 0）

### 开发需求

1. LRC 解析工具，生成结构化数据，支持时间校准；
2. OSC 客户端读取 Studio One 播放头时间 `T`（需文档化实际 OSC 地址/port 配置）；
3. 备选 MTC：LoopMIDI 虚拟端口接收 MIDI Time Code；
4. 传输延迟补偿逻辑，消除 OSC 微小偏移；
5. 根据 `T` 匹配歌词条目，向外推送文字 + 高亮索引信号。

### 验收标准

连接 Studio One 实时同步播放进度；LRC 精准匹配时间；悬浮窗高亮正常。

---

## 分段任务 7：中央调度核心层全量集成（最后串联全部模块）

### 开发需求

1. 在任务 0 骨架上扩展：统一管理所有子线程生命周期；
2. 中转全模块控制信号，统一管理全局状态（AI 启停、播放、模型切换、更新）；
3. 全局时间 `T` 统一分发至歌词模块；
4. 全局异常统一中转，自动触发日志上报；
5. 链路联动：启动跟唱自动初始化音频 + 推理线程，关闭有序释放 CUDA/音频资源；
6. 调度层仅消息转发与生命周期管理，不实现 MSST/RVC/歌词业务逻辑。

### 验收标准

所有模块接入后，离线翻唱、实时变声、歌词同步全链路打通；软件启停无内存泄漏、无 CUDA 显存残留。

---

# 六、打包、部署与自动更新架构

## 1. 打包方案

**首选**：Nuitka 编译打包  
**备选（推荐内测阶段）**：嵌入式 Python 目录 + 启动器 exe（与声迹 `msst-reflow/python-backend` 同类，CUDA/Torch 兼容性更好）

1. 完整内置依赖：Python 3.12、CUDA Torch、FFmpeg、RMVPE/Hubert/MSST 权重、音频底层库；
2. 输出独立单目录客户端，内置启动 exe；
3. 自动清理冗余缓存，区分开发测试包、正式发布包。

> **风险**：Nuitka + PyTorch CUDA + faiss 组合打包失败率较高，必须在任务 7 完成后单独安排 1~2 周打包专项。

## 2. 更新后端轻量化架构（Nginx / FastAPI 静态服务）

无需数据库，仅静态文件 + 简易上传接口。

### version.json 标准规范

```json
{
  "latest_version": "1.0.2",
  "force_update": false,
  "update_desc": "修复切换模型歌词错位 bug，优化低显卡推理速度",
  "full_package_url": "https://xxx/client_full_v1.0.2.zip",
  "patch_url": "https://xxx/patch_v1.0.1_to_v1.0.2.zip",
  "md_full": "文件MD5值",
  "md_patch": "补丁MD5值"
}
```

- 静态目录：版本配置、增量补丁、完整安装包；
- 日志 POST 接口：接收客户端崩溃日志、硬件信息归档存储。

## 3. 标准化发布流程

1. 迭代：单模块独立测试 → 调度层集成测试 → 打包测试包；
2. 内测：服务器上传测试包，修改 version.json 指向测试版本；
3. 正式发布：修改 latest_version，全量用户自动弹窗更新；
4. 故障回滚：version.json 指向历史稳定版本，用户重启自动回退。

---

# 七、开发迭代验收强制规范

1. 每个模块必须提供**独立启动脚本**，无需依赖未开发模块即可运行测试；
2. 同步输出单元测试代码，覆盖核心逻辑、异常场景；
3. 配套模块说明文档：入参、输出信号、依赖清单、功能边界；
4. 人工全场景测试无 bug、功能完全匹配文档，才可进入下一模块；
5. 全部模块完成后执行全链路集成测试；
6. 集成测试通过执行打包，验证安装、更新、回滚全流程。

---

# 八、后期扩展预留接口（架构提前兼容）

1. UI 国际化多语言抽象层预留；
2. 用户配置云端同步接口预留；
3. 在线音色模型下载商城接口预留；
4. 直播歌词在线样式资源加载接口预留；
5. macOS 客户端跨平台适配抽象层预留；
6. SoundTrail `.model` 导入转换器；
7. 自研实时 pitch correction（真正对标 AI 跟唱）；
8. ASIO 原生直连（绕过 Voicemeeter）。
