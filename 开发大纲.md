# RVC AI跟唱客户端（对标 SoundTrail 声迹）分层开发架构需求总纲

> 文档格式：Markdown  
> 技术栈：Python 3.12 + PyQt6 + 本仓库 RVC 内核 + MSST(pymss) + RMVPE + sounddevice + OSC/MTC + pywin32 + Nuitka  
> 运行平台：Windows 10/11  
> **产品主路径（对标声迹）**：离线预渲染 AI 人声 → 播放时 **VAD 门控 AI 人声**（预渲染轨按 T 播放，麦仅作开关）+ 伴奏混音 → 监听/OBS  
> **Phase 2 可选**：麦克风实时修音（参考 F0 贴合，原 `pitchfix` 修音引擎）  
> **辅路径**：麦克风实时 RVC（说话变声等）；可经 Voicemeeter → Studio One 做外置混音  
> 开发工具：Cursor 分段自动化开发、单模块独立测试、迭代验收  
> 交付产物：Windows 独立桌面客户端 | 内置远程崩溃日志上报 + 增量自动更新系统

## 审查修订记录（2026-08-04）

| 类别 | 原稿问题 | 修订说明 |
| --- | --- | --- |
| 产品定位 | 未说明与 SoundTrail 的差异 | 补充「对标范围」：离线做歌可对齐；实时跟唱本方案走**麦克风实时 RVC**，SoundTrail 走**预渲染 AI 人声 + 自研修音** |
| 离线链路 | 缺去混响阶段；`other` 误当和声 | 对齐声迹日志：分离 → 去混响 →（可选）和声提取 → RVC → 四轨导出 |
| 任务顺序 | 实时标最高优但 UI 先全量开发；调度层放最后 | 调整为：**调度骨架 → 离线链路（复用现有代码）→ 实时链路 → 歌词/运维 → 全量 UI 完善** |
| 模块依赖 | MSST 依赖 RVC 推理；离线 RVC 依赖音频 IO | 分离 MSST/RVC 离线/RVC 实时三条依赖线 |
| 采样率 | 全局写死 48000Hz | MSST 固定 44100Hz，RVC 按模型采样率；统一在 IO 层做重采样 |
| 实时缓冲 | 50ms 环形缓冲 | 改为 100~500ms 可配置，参考 `realtime_gui.py` / `RVCRealtimeVST` |
| 歌词 F0 约束 | 写「RVC 接收字级 F0 区间」 | 标注为**自研扩展**（标准 RVC 不支持），分 Phase 2 可选实现 |
| 代码复用 | 写「剥离 Gradio 重写」 | 明确复用 `tools/song_cover.py`、`tools/pymss_webui.py`、`tools/pymss/workflow.py`、`infer/rtrvc.py`、`RVCRealtimeVST/` |
| 打包 | 仅 Nuitka | 补充风险：CUDA+Torch 打包困难，备选嵌入式 Python 目录（与声迹 `msst-reflow` 同类方案） |
| 模型格式 | 支持 `.model` | `.model` 为声迹私有封装，首版仅支持 RVC 标准 `.pth` + `.index` |

## 审查修订记录（2026-08-05）

| 类别 | 问题/结论 | 修订说明 |
| --- | --- | --- |
| AI 跟唱路径（早间） | 曾写「麦克风实时 RVC」为主 | **已废止为主目标**（见下条） |
| **AI 跟唱路径（目标修正）** | 用户目标对齐声迹 | **主目标改为：预渲染 AI 人声 + 内置实时修音**；实时 RVC 降为「说话变声」辅路径；新增分段任务 **9** |
| 歌词格式 | 传统行级 LRC 无法支撑逐字高亮/修词 | **Phase 1** 行级 LRC（已实现）；**Phase 2** 落盘/交换用 **Enhanced LRC**，内存用字级结构 |
| 歌词来源 | Enhanced LRC 网上难找；声迹疑似平台下词+缓存 | **主路径：人声轨自动对齐生成**（Whisper 等），不依赖酷狗 KRC；禁止以加密 KRC 为主格式 |
| 修词 | 播放页「改词」仅占位 | 新增 **分段任务 8**：修词 UI + 时间重映射/局部重对齐 + 写回 Enhanced LRC |
| 字级 F0 | 与修音相关但独立 | 修音引擎用参考 F0 曲线（可来自 AI 人声轨）；歌词字级时间轴可选辅助，非必须 |

## 审查修订记录（2026-08-06）

| 类别 | 结论 | 修订说明 |
| --- | --- | --- |
| **明确不做（排最后）** | 云下载 Tab、主题切换、`.model` 私有模型 | 写入 §3.1；不纳入 Phase 2 验收 |
| **VST3 策略** | 声迹 Pro「插件宿主」≠ 本仓库已有 RVC VST 插件 | §3.2：已有 `RVCRealtimeVST`（变声插件）；**Phase 2 不做**客户端内置通用 VST3 宿主；过渡用 Voicemeeter Potato + Studio One |
| **外置联调** | 1:1 复刻前可直播 | 文档 `docs/cn/StudioOne_Voicemeeter_Potato联调指南.md` |
| **任务 9 MVP** | `app/pitchfix/` 首版已落地 | librosa pyin 参考 F0 + 块级 pitch_shift；F0 缓存 `.f0.npz`；后台线程加载，禁止阻塞 UI |
| **AI 跟唱 vs 变声** | 播放页「AI 跟唱」= 修音；托盘「启停 AI」= 实时 RVC | 路由分离，不得混验 |

## 审查修订记录（2026-08-08）

| 类别 | 结论 | 修订说明 |
| --- | --- | --- |
| **AI 跟唱 Phase 1** | 对标声迹当前实现 | **主输出改为：伴奏 + VAD 门控预渲染 AI 人声**；麦 RMS + 阈值/衰减作开关，**不识别音准**；哼/说/吹气均可触发 |
| **AI 跟唱 Phase 2** | 条件允许再集成 | 保留 `f0_curve.py` / `corrector.py` / `detune.py`；可选 `pitchfix.follow_engine=pitch_correct` 接入麦实时修音 |
| **🔊 混音面板** | 文案对齐 VAD | 「AI人声音量」= 门控轨增益；「跟唱阈值/衰减」= VAD 灵敏度；移除跑调模式 UI（Phase 2） |
| **F0 缓存** | 跟唱 Phase 1 不依赖 | 做歌末尾 `.f0.npz` 仍可生成，供 Phase 2 修音复用，非 Phase 1 启动条件 |

---

## 文档使用说明（给 Cursor）

1. 本文档为项目全局唯一总纲，所有开发逻辑、分层规则、功能需求均以此为准，不可偏离；
2. 分段任务 **0~7** 为 Phase 1（骨架、离线做歌、行级歌词、实时 RVC 辅路径）；**任务 8** 歌词逐字/修词；**任务 9** 内置实时修音（**AI 跟唱主路径**）。任务 9 依赖离线四轨与播放时钟，可与任务 8 并行规划，但建议先有稳定播放头 `T`；单个模块独立可运行测试、验收合格后再进入下一模块；
3. 全程遵守四层分层解耦规范：**控制消息**走调度层，**音频 PCM 热路径**可在内核层直连（见下文例外）；
4. 优先复用本仓库已有实现，禁止重复造轮子；新增代码保持最小 diff。
5. 尽量不要修改模块内已有代码，新功能请写新模块，因为本项目还在持续开发中，我会定期同步上游源代码的bug以及新功能
6. 尽量增加文档注释，保证代码可读性；编写代码时要遵循**统一风格**，统一代码风格、统一注释风格、统一命名风格、统一模块结构、统一模块依赖、统一模块测试、统一模块文档；

---

# 一、整体四层分层架构（强制全局标准）

自上而下分层，每层职责完全隔离，单模块可独立开发、单元测试，互不阻塞迭代。

## 1. UI 表现层（PyQt6 桌面界面）

- 仅负责窗口渲染、控件交互、数据可视化展示；
- 无 AI 推理、音频 IO 阻塞、大文件读写等耗时业务逻辑；
- 统一 Qt 信号槽异步通信，不使用全局变量传递状态数据。

## 2. 业务调度核心层（全局单例调度总线）

- 系统统一**控制消息**中转中心，串联 UI、音频、RVC 推理、歌词、更新、日志全部模块；
- 统一管控所有子线程生命周期，程序退出安全释放 CUDA 显存、音频流资源；
- 全局唯一时间基准分发：Studio One OSC/MTC 播放头时间戳 `T`；
- 统一中转异常、进度、状态信号，统一分发至 UI 与日志上报模块。

> **热路径例外（低延迟强制）**  
> 1. **AI 跟唱（主）**：麦克风 PCM → VAD 门控；converted_vocal + instrumental 按 T 混播；控制指令走调度层，PCM 不经调度层转发。  
> 2. **说话变声（辅）**：麦克风 PCM → 环形缓冲 → 实时 RVC → 输出缓冲；同上，调度层只负责启停/参数/切模型。

## 3. 底层能力内核层（可独立抽离 Python 库，无界面可单独测试）

拆分为 6 个独立子模块：

1. **音频 IO 硬件模块**：声卡/Voicemeeter/ASIO 枚举、环形音频缓冲、流式采集输出、歌库多轨播放
2. **RVC 推理内核模块**：离线批量翻唱（预渲染主路径）；实时流式转换（说话变声辅路径）；RMVPE 音高提取
3. **MSST 人声分离内核模块**：原唱人声/伴奏/和声/去混响，两套业务参数预设
4. **实时修音内核模块（Phase 2 / 任务 9 可选）**：参考 F0 曲线 + 麦克风 pitch correction；与 VAD 门控跟唱并列，非 Phase 1 必需
5. **歌词同步时钟模块**：行级/字级歌词解析、OSC/MTC/本机播放头时间、悬浮歌词；（任务 8）对齐生成 Enhanced LRC、修词
6. **系统运维工具模块**：硬件检测、分级本地日志、远程崩溃上报、客户端自动更新

## 4. 底层依赖基础层（统一环境管理）

FFmpeg、PyTorch(CUDA)、sounddevice、LoopMIDI、OSC 通信库、pywin32、Nuitka 打包工具；（Phase 2 歌词对齐可选）faster-whisper / openai-whisper 等 ASR 对齐依赖

### 本仓库可直接复用的现有资产

| 模块 | 路径 | 用途 |
| --- | --- | --- |
| 一键翻唱离线链路 | `tools/song_cover.py` | 分离 → RVC → 混音，可扩展为多阶段 |
| MSST WebUI 封装 | `tools/pymss_webui.py` | 分离/去混响/提主旋律模型与进度事件 |
| MSST 多步工作流 | `tools/pymss/workflow.py` | YAML 编排分离→去混响→和声 |
| 实时 RVC 核心 | `infer/rtrvc.py` | 流式推理、formant、RMVPE |
| 实时 GUI 参考 | `realtime_gui.py` | 块大小、formant、设备参数 |
| 实时 VST 参考 | `RVCRealtimeVST/worker/rvc_worker.py` | 共享内存 IPC、无断音切换 |

---

# 二、全局统一强制规范（所有模块必须遵守）

1. 所有耗时任务（AI 推理、模型加载、音频分块、网络更新）全部放入独立子线程，禁止阻塞 UI 主线程；
2. **控制状态**（启停、进度、异常、参数）仅通过异步信号传递；音频 PCM 热路径允许内核层环形缓冲直连；
3. 全局唯一时间基准：Studio One 输出播放头秒级时间戳 `T`，歌词渲染依赖 `T`；RVC 不参与时间计算；
4. 音频隔离规则：**伴奏全程不进入 RVC 推理**；离线做歌仅 MSST 干人声进 RVC；**AI 跟唱 Phase 1** 输出为 VAD 门控的预渲染 AI 人声 + 伴奏；**Phase 2 可选**麦修音；**说话变声**仅麦克风进实时 RVC；
5. 采样率策略：MSST 内部固定 **44100Hz**（见 `tools/pymss_webui.py`）；RVC 按模型 `sampling_rate`（常见 40000/48000）；音频 IO / 修音默认 **48000Hz**，边界处显式重采样，禁止隐式假设全链路同采样率；
6. 用户配置持久化：声卡参数、RVC 推理参数、修音强度、界面布局、更新地址、热键全部存入本地 JSON，开机自动加载；
7. 日志分级：`DEBUG / INFO / WARN / ERROR`；本地滚动分文件存储，程序异常自动打包日志远程上报。

---

# 三、对标 SoundTrail 的范围说明

| 能力 | SoundTrail 实现 | 本客户端目标 | 阶段 |
| --- | --- | --- | --- |
| 离线做歌 | MSST 多阶段 + RVC + 四轨导出 | 对齐（见链路 2） | ✅ Phase 1 |
| AI 唱歌 | 预渲染 AI 人声轨播放 | 播放 `converted_vocal`（可混伴奏） | ✅ Phase 1 |
| **AI 跟唱** | 预渲染 AI 人声 + **VAD 门控**（麦作开关） | **同路径 Phase 1**：`app/pitchfix/` VAD 门控 `converted_vocal` | ✅ **任务 9 Phase 1** |
| **AI 跟唱修音** | 内置实时修音（可选增强） | Phase 2：`corrector` + RMVPE F0 | ⏳ 条件允许再集成 |
| 说话变声 | — | 麦克风 **实时 RVC**（辅路径，非跟唱验收标准） | ✅ Phase 1 代码有；联调可选 |
| 四轨 | 伴奏/人声/原唱/和声 | 同上 | ✅ Phase 1 |
| 歌词悬浮（行级） | 多格式 + 时间同步 | 标准 LRC + 播放头/OSC + 悬浮窗 | ✅ Phase 1 |
| 逐字高亮 / 嘴型 | 宣传逐字、5ms 级 | Enhanced LRC + 字级 tick；人声自动对齐生成 | ⏳ 任务 8 |
| 修词 / 改词轨 | 有改词编辑与播放能力 | 编辑文本 → 重映射/局部重对齐 → Enhanced LRC | ⏳ 任务 8 |
| `.model` 模型包 | 私有格式 | 首版仅 `.pth` + `.index` | ⏳ 后续 |
| 酷狗转码 / 平台下词 | 产品能力 | **不做** KRC 解密主路径 | ❌ 非目标 |

### 3.1 明确不做 / 排最后（低优先级）

| 能力 | 说明 |
| --- | --- |
| **云下载 Tab** | 声迹官网有，本客户端不做；曲库链接下载列为远期 |
| **主题切换**（莫兰迪等） | 不做；沿用当前固定 UI 风格 |
| **`.model` 私有模型包** | 不做导入；仅 RVC 标准 `.pth` + `.index` |

### 3.2 VST3 策略（与声迹 Pro 架构差异）

| 类型 | 含义 | 本仓库 | 阶段 |
| --- | --- | --- | --- |
| **A. RVC 作为 VST3 插件** | RVC 装进 Studio One 等 DAW | ✅ `RVCRealtimeVST/` | 辅路径，已可编译 |
| **B. 客户端内置 VST3 宿主** | 在 PyQt 内加载 FabFilter 等效果器 | ❌ Phase 2 不做 | Phase 3+ 或永不做 |

**过渡方案（当前推荐）**：Voicemeeter **Potato** + Studio One + RVC VST3 插件 + 本客户端（离线做歌 / AI 唱歌 / **内置 AI 跟唱修音**）。详见 `docs/cn/StudioOne_Voicemeeter_Potato联调指南.md`。

**最终目标**：架构 1:1 复刻声迹 Pro（内置修音 + 多轨 + 可选宿主）分阶段实现；外置联调经验可复用。

### AI 跟唱实现思路（主目标，对标声迹）

```text
【Phase 1 — 当前实现 ✅】
  输出 = 伴奏(instrumental) × 伴奏音量
       + converted_vocal(按播放头 T) × VAD门控 × AI人声音量
       + converted_vocal × 原唱监听（可选，不参与 VAD）
  VAD：麦 RMS vs 跟唱阈值；停麦 hangover 由跟唱衰减控制；不识别音准

【Phase 2 — 后续可选】
  在 Phase 1 基础上，麦 PCM → 修音引擎（目标 F0 = converted_vocal 在 T 处）
  → 替换或叠加门控 AI 人声；复用 f0_curve / corrector / detune

【主路径流程】
  1) 离线：MSST + RVC → converted_vocal + instrumental 入库
  2) 选歌：本机播放头 T；播伴奏；converted_vocal 按 T 读取
  3) 跟唱：麦有声音 → 门控打开 → 输出 AI 人声；无声音 → 门控关
  4) 混音 → 声卡 / Voicemeeter / OBS；歌词 T 驱动高亮

【辅路径 — 说话变声】麦克风 → 实时 RVC（4B）；不作为 AI 跟唱验收标准

【实现位置】`app/pitchfix/service.py` — PitchFollowService（VAD 门控版）
```

> **原则**：伴奏不进 RVC；Phase 1 跟唱**不跑修音**，预渲染 AI 人声即主输出轨；歌词 `T` 与播放共用，不反向驱动离线推理。

---


# 四、四大核心业务链路完整需求

## 链路 1：AI 跟唱 / 播放直播链路（主路径 = 预渲染 + VAD 门控）

### 1A 主路径：预渲染 AI 人声 + VAD 门控（对标声迹 Phase 1，任务 9）

```text
选歌（已有 converted_vocal / instrumental）
  → 本机多轨播放：伴奏必播；converted_vocal 按播放头 T 读取
  → 麦克风采集 → VAD（RMS 阈值 + hangover 衰减）→ 门控系数 0/1
  → 输出 = 伴奏 + converted_vocal(T) × 门控 × AI人声音量
  → 声卡 / Voicemeeter → OBS
  → 同时：T → 歌词高亮
```

| 模式（播放页） | 行为 |
| --- | --- |
| AI 唱歌 | 播预渲染 `converted_vocal` + 伴奏；麦可关或仅监听 |
| AI 跟唱 | **VAD 门控**预渲染 AI 人声 + 伴奏；麦仅作开关，不修音 |
| 混响说话 / 普通说话 | 麦直通或轻处理；不走跟唱 |
| 说话变声（辅） | 现有实时 RVC（链路 1B） |

> **Phase 2 可选**：在 1A 基础上叠加「麦 → 修音 → 输出」，见 §任务9 Phase 2。

### 1B 辅路径：麦克风实时 RVC（说话变声；已有代码）

物理麦克风 →（可选 Voicemeeter）→ 音频 IO → RVC 流式推理 → 输出 →（可选 Studio One）

> **⏳ 联调备注**：实时 RVC 曾出现 Voicemeeter 反馈环（输出勾 B1）。辅路径联调原则：仅麦进采集；RVC 输出禁止回灌采集总线。  
> **配置参考**：`python scripts/list_audio_devices.py`；历史 `input_device`/`output_device` 见 `config/client.json`。

### 配套联动逻辑

1. 全局播放头时间 `T`：优先本机播放器时钟；可选 Studio One OSC/MTC 外同步；
2. 歌词按 `T` 高亮（Phase 1 行级 / 任务 8 字级）；
3. AI 跟唱启停与选歌、伴奏/AI 人声预加载绑定；切歌须重载 converted_vocal；
4. 全局热键：跟唱启停、唱歌/跟唱模式切换、修音强度；
5. 系统托盘常驻；缓冲 **100~500ms** 可配置（修音与 RVC 辅路径均适用）。

## 链路 2：离线 AI 翻唱制作链路（第二阶段开发，复刻声迹「制作歌曲」，**优先开发**）

### 业务流程（对齐声迹日志）

原唱音频导入 → FFmpeg 解码 → **Stage1** MSST 人声/伴奏分离 → **Stage2** MSST 人声去混响 → **Stage3（可选）** MSST 提主旋律/和声分离 → RVC 批量转换干人声 → 导出四轨 + 可选 FFmpeg 合并成品

```text
输入整曲
  → vocals.wav          （原唱人声，保留）
  → instrumental.wav    （伴奏）
  → vocals_noreverb.wav （去混响干声，RVC 输入）
  → harmony.wav         （和声，karaoke 模型 off_vocal/other）
  → converted_vocal.wav （RVC 输出 AI 人声）
  → cover.wav           （可选：AI 人声 + 伴奏混音）
```

### 两套分离预设（对标普通/强力做歌）

| 预设 | Stage1 分离 | Stage2 去混响 | Stage3 和声 | 说明 |
| --- | --- | --- | --- | --- |
| 普通做歌 | `去伴奏` bs_roformer_368 | `去混响` less-aggressive | 跳过 | 速度快，适合直播素材 |
| 强力做歌 | `去伴奏（激进）` bs_roformer_317 | `去混响（激进）` | `提主旋律` karaoke | 质量优先，输出四轨 |

> **纠正**：Stage1 的 `other/instrumental` 是「非人声剩余轨」，**不是和声**；和声需 Stage3 karaoke 模型单独提取。

### 配套功能

1. 音频分块处理，实时进度日志，支持中途终止任务（复用 `pymss_separate` 事件回调）；
2. RVC 推理前 `_release_vc_gpu` / 分离后 `_restore_vc_gpu`（见 `tools/song_cover.py`，防 OOM）；
3. 自动识别显卡算力，算力不足自动降级 CPU 推理并弹窗提示；
4. 工程集合保存，批量存储多首歌曲参数，可重复加载复用；
5. 首版在 `tools/song_cover.py` 上扩展，不另起炉灶。
6. （Phase 2）做歌完成后可选：对人声轨跑对齐 → 生成 Enhanced LRC 入库（见任务 8）。

## 链路 3：歌词同步联动子系统（Phase 1 已实现骨架；Phase 2 扩展逐字/修词）

独立后台线程运行，不占用 UI、AI 推理资源。

### Phase 1（当前，标准 LRC + 行高亮）— ✅ 已完成代码验收

1. 标准 LRC 拖拽/同目录导入、`offset_ms` 时间轴校准；结构化：`[起始秒, 结束秒, 文本]`（见 `app/lyrics/`）；
2. 双同步：**优先 Studio One OSC**，备选 LoopMIDI MTC（骨架）；播放页可用 manual 时钟跟播；
3. Qt 悬浮歌词窗：无边框、置顶、拖拽；调度总线推送 `lyric_tick`（行索引 + 文本）；
4. 不解析/不渲染字级标签；不依赖平台歌词下载。

### Phase 2（后期，Enhanced LRC + 自动对齐 + 修词）— ⏳ 分段任务 8

**格式约定**

| 层级 | 约定 |
| --- | --- |
| 落盘 / 交换 | **Enhanced LRC**（行标签 `[mm:ss.xx]` + 字标签 `<mm:ss.xx>`）；UTF-8 |
| 内存 | `LyricLine` + `LyricWord(start, end, text)`；UI/修词编辑此结构 |
| 导入兼容 | 标准行级 LRC 仍可导入；无字时间戳时先均分或触发对齐补齐 |
| 不做主路径 | 加密 KRC/QRC 解密、酷狗私有协议 |

**歌词从哪来（不靠网上找 Enhanced LRC）**

```text
人声轨（优先 vocals_noreverb / vocals）+ 可选纯文本或行级 LRC
  → ASR/强制对齐（如 faster-whisper；可换 MFA 等）
  → 字/词级时间轴
  → 导出 Enhanced LRC 入库（可挂在离线做歌完成后可选步骤）
```

**修词流程**

1. UI 改字/改句；
2. 句长接近 → 按旧时间戳重映射；字数差大 → **局部重对齐**（用人声轨）；
3. 写回 Enhanced LRC；`lyric_tick` 升级为可带字级索引；
4. 「改词轨音频」（TTS/重唱后再对齐）列为更后期，与「只改字幕」分开。

**可选**：字级 F0 区间约束下发 RVC（非 RVC 原生，独立自研）。

## 链路 4：软件运维子系统（与任务 1 并行，发布部署必备）

### 4.1 远程 Bug/异常上报

1. 全局捕获程序崩溃、CUDA 报错、声卡断开、IO 异常，自动抓取完整堆栈；
2. 本地日志自动打包，HTTP POST 上传远程日志服务；
3. 支持用户手动一键导出日志压缩包线下排错；
4. 上报携带环境信息：软件版本、Windows 版本、显卡型号、CUDA 算力、音频设备参数。

### 4.2 客户端自动更新

1. 启动后台线程请求远程 `version.json` 版本接口，对比本地版本；
2. 双更新策略：小版本增量补丁、大版本完整客户端包；
3. MD5 文件完整性校验，下载失败自动重试；更新完成弹窗提示重启；
4. 更新失败自动回滚旧版本，保证客户端不损坏；
5. 远程可配置：更新地址、强制更新开关、版本公告文本。

---

# 五、分模块开发任务清单（Cursor 分段执行，独立测试验收）

> **推荐开发顺序**：  
> **Phase 1**：0 → 2 → 5 → 4-离线 → 1(骨架) → 3 → 4-实时(辅) → 6 → 7 → 播放稳定 / OSC 可选  
> **Phase 2（产品跟唱）**：**9 内置实时修音（AI 跟唱主路径）** ↔ 8（Enhanced LRC/修词，可并行）→ 打包

## 分段任务 0：调度层最小骨架 + 配置/日志基座（最先开发）

### 开发需求

1. 调度器单例：线程注册/启停/退出钩子、信号中转（不含业务逻辑）；
2. JSON 配置读写、日志分级类；
3. 模块间信号协议定义（Typed dataclass 或 Enum，避免字符串魔法值）。

### 验收标准

独立脚本可启动调度器、写入日志、读写配置、模拟信号转发；无 UI、无音频。

---

## 分段任务 1：系统运维工具模块（可与任务 0 并行）

### 开发需求

1. 分级日志类：自动滚动分割、单文件大小限制、自定义存储路径；
2. 全局异常捕获装饰器：捕获 Python、CUDA、音频硬件异常；
3. 远程日志上报 HTTP 客户端，自动携带设备环境信息；
4. 自动更新工具类：版本请求、MD5 校验、增量下载、更新替换、版本回滚；
5. 硬件采集工具：读取 Windows、显卡、CUDA、全量音频设备列表。

### 验收标准

独立脚本可测试日志写入、硬件读取、版本校验、模拟异常自动打包日志；更新下载、校验、回滚流程完整。

---

## 分段任务 2：PyQt6 UI 表现层（骨架优先，细节后补）

> **结构修订（2026-08-04）**：主界面对标 SoundTrail **一级三 Tab**，而非把「实时/离线/模型/设置」当作顶层 Tab。  
> 离线做歌、实时跟唱等能力分别落在对应 Tab 内，后续阶段再接入业务逻辑。

### 顶层 Shell（`app/ui/main_window.py`）

1. **顶栏导航**：`播放` | `制作歌曲` | `公告`（`HeaderBar` + `QStackedWidget`）
2. **底栏**：左侧状态消息 + 右侧 GPU 监控 + 插件连接占位
3. **底日志区**：开发期运行日志（SoundTrail 无此区域，可后续折叠）
4. **设置入口**：顶栏「设置」按钮 → 对话框（OSC/更新/日志路径、模型导入）
5. 窗口几何与当前 Tab 写入 `config/client.json` 的 `ui_layout`

### Tab 1：播放（`app/ui/pages/playback_page.py`）— 骨架

| 区域 | 内容 | 接入阶段 |
| --- | --- | --- |
| 左栏 | 我的歌库、搜索、歌曲列表 | 任务 7 |
| 中栏 | 播放显示、波形；**AI 唱歌**（预渲染）；**AI 跟唱**（任务 9 修音）；说话变声（4B 辅） | 任务 7 / 9 / 4B |
| 右栏 | 歌词列表（行级）；**改词编辑 → Phase 2 任务 8** | 任务 6 / 8 |

### Tab 2：制作歌曲（`app/ui/pages/song_make_page.py`）— 骨架（当前重点）

| 区域 | 内容 | 接入阶段 |
| --- | --- | --- |
| 左栏 | 伴奏/人声/原声/歌词资源格、已加载文件、保存集合/清理 | 任务 5/7 |
| 中栏 | 「输入文件做歌 / 输入曲库链接做歌」、文件列表与选择操作 | 任务 5/7 |
| 右栏 | 分离伴奏开关、普通/强力做歌、音高/粗细滑条、高级 RVC 参数、开始处理 | 任务 4A/5/7 |

> 实时跟唱**不在此 Tab**；在「播放」Tab 通过 AI 跟唱等按钮进入（任务 4B）。

### Tab 3：公告（`app/ui/pages/announce_page.py`）— 骨架

- 左：公告列表 + 刷新
- 右：公告详情（后续接远程 `version.json` 公告字段）

### 全局附属窗口

1. **悬浮歌词子窗口**（`lyrics_window.py`）：无边框、置顶、拖拽 — 任务 6 完善
2. **系统托盘**（`tray.py`）：启停 AI、打开主界面、退出、检查更新
3. UI 全部经 `UiBridge` → `AppScheduler`，界面无 MSST/RVC 阻塞调用

### 验收标准

1. `python scripts/test_task2_ui.py` 通过（3 个顶层页、无头构建）
2. `python scripts/run_ui_skeleton.py` 可切换三 Tab，制作歌曲三栏布局可见
3. 控件点击仅 emit 占位 action / 友好提示，无崩溃
4. 悬浮窗、托盘可用（系统托盘不可用时跳过）

---

## 分段任务 3：音频硬件 IO 调度模块（依赖任务 0，独立声卡测试）

### 模块结构（`app/audio/`）

| 文件 | 职责 |
| --- | --- |
| `devices.py` | 枚举 HostAPI/设备，标记 Voicemeeter/VB-Cable/WASAPI |
| `ring_buffer.py` | float32 环形缓冲，供回调与业务线程交换 PCM |
| `stream_manager.py` | `AudioStreamManager`：duplex 流、100~500ms block、热插拔重连、欠载 fade |
| `service.py` | `AudioService`：对接 `AppScheduler`，启停流/枚举设备 |

配置项写入 `config/client.json` → `audio.*`（采样率、block_ms、设备索引、ring_ms 等）。

### 开发需求

1. 封装 sounddevice，枚举 WDM/WASAPI 设备，自动识别 Voicemeeter/VB-Cable 虚拟声卡并标记；
2. 音频流管理类：创建/启停/销毁，内置 **100~500ms 可配置**环形缓冲池；
3. 音频 IO 默认：48000Hz、float32（内部计算）/ int16（设备输出可选），缓冲块可调；
4. Voicemeeter 通道专用识别逻辑，区分硬件输入/VAIO 人声/AUX 伴奏；
5. 音频容错：热插拔自动重连、爆音平滑；**不承诺关闭系统级独占模式**（取决于驱动，仅检测并提示）；
6. 独立子线程采集音频，PCM 写入内核环形缓冲；控制信号经调度层。

> **ASIO**：首版可不做 ASIO 原生支持，Voicemeeter 已可桥接 ASIO 设备；ASIO 直连列为后期扩展。

### 验收标准

1. `python scripts/test_task3_audio.py` 通过（环形缓冲、设备枚举、调度集成）
2. 可选 `python scripts/test_task3_audio.py --live 3`：passthrough 流稳定回调
3. 插拔/异常时 watchdog 重连并记录 stats，无持续爆音（欠载 fade）

---

## 分段任务 4：RVC 推理内核模块（拆为 4A 离线 / 4B 实时）

基于本仓库 `infer/rtrvc.py`、`infer/vc/` 封装纯 Python 调用类，**禁止重写 Gradio 已有逻辑**。

### 4A 离线批量推理（依赖任务 5 产出干声，**不依赖**任务 3 音频 IO）

1. 扩展 `tools/song_cover.py`：多阶段 MSST + 四轨导出 + 可选混音；
2. 分块转换，对接 FFmpeg 合并/导出；
3. 参数映射：音调偏移、formant（声音粗细）、Index 比例、protect、最小/最大 F0、说话人 ID、RMVPE/FCPE；
4. 推理进度、GPU 占用异步推送 UI 日志。

### 4B 实时流式推理（依赖任务 3；**说话变声辅路径**，非 AI 跟唱主验收）

| 文件 | 职责 |
| --- | --- |
| `realtime_config.py` | 块大小、crossfade、F0 等实时参数 |
| `realtime_engine.py` | `RealtimeRvcEngine` + `RealtimeModelPool`（封装 `infer.rtrvc.RVC` + SOLA） |
| `realtime_service.py` | 推理线程：`input_ring` → RVC → `output_ring`，对接调度层 |

1. 多模型后台预加载（`RealtimeModelPool.preload/switch`）；
2. 环形缓冲平滑切换，无断音热切换（复用 `last_rvc` HuBERT 共享）；
3. RMVPE F0、HuBERT 特征、RVC v2 完整管线；
4. 硬件自动调度：检测 CUDA，无 N 卡切 CPU，低算力显卡弹窗提示（沿用 `configs.config.Config`）；
5. **AI 跟唱主路径见分段任务 9**（预渲染 + 内置修音），勿与本 4B 混为同一产品能力。

### 验收标准

4A：`python scripts/test_task4_offline.py --live` 完成离线四轨导出。  
4B：`python scripts/test_task4_realtime.py` 块级推理通过；可选 `--live 5` 麦克风实时链路演示。

---

## 分段任务 5：MSST 人声分离内核模块（依赖 FFmpeg + 任务 0，**不依赖 RVC**）

### 开发需求

1. 封装 `tools/pymss_webui.py` / `tools/pymss/workflow.py`，实现「普通做歌/强力做歌」两套预设（见链路 2 表格）；
2. FFmpeg 解码全格式音频，输出标准 WAV（44100Hz）；
3. 大音频分块处理，限制内存，实时推送进度信号（复用 `on_separate_event`）；
4. 支持中途终止，自动清理临时缓存；
5. 分离完成推送文件路径信号至调度层。

### 验收标准

导入原唱自动完成 Stage1+2（强力含 Stage3）；两套模式输出文件结构不同；中断任务无残留缓存文件。

---

## 分段任务 6：歌词同步 & Studio One 时间同步模块（依赖任务 0）— Phase 1 ✅

### 模块结构（`app/lyrics/`）

| 文件 | 职责 |
| --- | --- |
| `lrc_parser.py` | **标准行级** LRC 解析、整体时间轴 shift（Phase 1） |
| `matcher.py` | 根据 T + offset_ms 匹配当前行 |
| `osc_client.py` | Studio One OSC UDP 播放头（`OscPlaybackClock`） |
| `mtc_reader.py` | LoopMIDI MTC 备选时钟（骨架） |
| `service.py` | `LyricsService`：tick 线程 → 调度总线 `lyric_tick` |
| `types.py` | `LyricLine` / `LyricDocument`（Phase 2 扩展 `LyricWord`） |

配置：`config/client.json` → `lyrics.*`（osc_port、osc_addresses、offset_ms、clock_source）。

Studio One 需在 OSC 面板将「播放时间（秒）」映射到本机 `lyrics.osc_port`，地址与 `osc_addresses` 一致。

### 开发需求（Phase 1）

1. 标准 LRC 解析，结构化数据，支持 `offset_ms`；
2. OSC 客户端读取 Studio One 播放头时间 `T`；
3. 备选 MTC：LoopMIDI 虚拟端口接收 MIDI Time Code；
4. 传输延迟补偿：`LyricMatcher.offset_ms`；
5. 根据 `T` 推送 `lyric_tick`（行文字 + 高亮索引）。

### 验收标准（Phase 1）

1. `python scripts/test_task6_lyrics.py` 通过（解析、匹配、仿真时钟）
2. 可选 `pip install python-osc && python scripts/test_task6_lyrics.py --osc`
3. `run_ui_skeleton.py` 启动后悬浮窗可接收 `lyric_tick` 高亮

> Phase 2 逐字/修词见 **分段任务 8**，勿在任务 6 内提前大改解析器破坏行级兼容。

---

## 分段任务 7：中央调度核心层全量集成（最后串联全部模块）— Phase 1

### 模块结构（`app/integration/`）

| 文件 | 职责 |
| --- | --- |
| `controller.py` | `ClientController`：UI action → 离线/实时/歌词/运维 |
| `state.py` | 全局运行状态（idle / offline / realtime） |

入口脚本：`scripts/run_ui_skeleton.py`（已接入 `ClientController`）。

### 路由表（UI action → 链路）

| action | 行为 |
| --- | --- |
| `offline_cover` | 后台线程 `OfflineSongPipeline` |
| `playback_ai_sing` | 播放预渲染 `converted_vocal` + 伴奏 |
| `playback_ai_follow` | **`PitchFollowService`（任务 9 Phase 1）**：VAD 门控 converted_vocal + 伴奏 |
| `ai_toggle` | 托盘：**实时 RVC 变声**（`RealtimeRvcService`，辅路径 4B，≠ AI 跟唱） |
| `playback_*_talk` | 音频直通（不经 RVC/修音） |
| `lyrics_load` / `lyrics_start` | LRC + 时钟同步 |
| `check_update` | 运维更新检查 |

### 开发需求

1. 统一管理子线程生命周期（离线 / 实时 / 歌词）；
2. 中转控制信号与全局状态；
3. 异常 → `SignalType.ERROR` + UI 日志；
4. 退出时 `shutdown` 释放音频与推理资源。

### 验收标准

1. `python scripts/test_task7_integration.py` 通过
2. `python scripts/run_ui_skeleton.py`：制作歌曲「开始处理」、播放「AI 跟唱」、托盘启停有真实日志反馈
3. 退出客户端无残留报错（CUDA/音频由 shutdown hook 释放）

### 当前进度备注（2026-08-07）

| 链路 | 状态 |
| --- | --- |
| 离线做歌（预渲染四轨） | ✅ 已跑通（MSST + RVC）；✅ 做歌末尾子进程生成 `.f0.npz` |
| 调度集成 / UI 骨架 | ✅ 任务 0~7 代码完成 |
| 歌词 Phase 1（行级 LRC） | ✅ 脚本验收；OSC 真机联调 ⏳ |
| 歌词任务 8（Enhanced / 修词） | ⏳ 未开工 |
| **AI 跟唱（任务 9 Phase 1）** | ✅ VAD 门控预渲染 AI 人声；🔊 混音面板；Phase 2 修音/跑调待集成 |
| 说话变声（实时 RVC 4B） | ✅ 托盘 `ai_toggle` |
| 外置 VM Potato + S1 + RVC VST3 | 📄 `docs/cn/StudioOne_Voicemeeter_Potato联调指南.md` |
| 云下载 / 主题 / `.model` | ❌ 不做（排最后） |
| 内置 VST3 宿主 | ❌ Phase 2 不做 |

### 建议下一步

1. 任务 9 未完成项（§分段任务 9）  
2. 任务 8 Enhanced LRC  
3. OSC / 打包  
4. 播放设置独立 Tab（跑调/智能切换等从 🔊 迁出）

---

## 分段任务 8：Enhanced LRC / 人声对齐 / 修词（Phase 2，依赖任务 6/7）

> **开工条件**：任务 0~7 稳定；行级歌词与播放链路可用。不改动上游 RVC/MSST；新能力落在 `app/lyrics/` 扩展模块。

### 模块结构（建议新增）

| 文件 | 职责 |
| --- | --- |
| `types.py` | 扩展 `LyricWord`；`LyricLine.words` |
| `enhanced_lrc.py` | Enhanced LRC 读/写（兼容标准 LRC） |
| `aligner.py` | 人声轨 → 字级时间轴（Whisper/faster-whisper 等，可替换） |
| `rewrite.py` | 修词：文本变更 → 重映射或触发局部重对齐 |
| `matcher.py` | 扩展字级匹配；`lyric_tick` 可带 `word_index` |
| UI | 播放页右栏改词编辑；悬浮窗逐字高亮 |

### 开发需求

1. 解析/导出 Enhanced LRC；标准 LRC 导入仍可用；
2. 离线做歌或选歌后可选「生成字级歌词」：输入 `vocals_noreverb`（或 vocals）+ 可选文本；
3. 修词 UI：改文本 → 重映射/局部对齐 → 写回同目录 `.lrc`（Enhanced）；
4. 播放与悬浮窗支持逐字高亮；行级模式保持兼容；
5. **不做**：酷狗 KRC 解密、平台爬虫下词作为主路径。

### 验收标准

1. `python scripts/test_task8_lyrics_enhanced.py`：Enhanced 解析往返、对齐结果结构、修词重映射单测通过  
2. 可选 `--live`：对人声样例生成 Enhanced LRC 并可加载高亮  
3. UI：改词保存后播放字级 tick 正确；无字级文件时退回行级

---

## 分段任务 9：AI 跟唱 / VAD 门控（Phase 1）+ 可选实时修音（Phase 2）

> **Phase 1 目标（2026-08-08 ✅）**：对标声迹「预渲染 AI 人声 + 麦 VAD 门控」。  
> **Phase 2 目标（后续）**：条件允许时集成麦实时修音（原 `corrector` + F0 曲线）。

### 模块结构（`app/pitchfix/`）

| 文件 | 职责 | 阶段 |
| --- | --- | --- |
| `service.py` | `PitchFollowService`：伴奏 + converted_vocal 按 T + VAD 门控混音 | ✅ Phase 1 |
| `f0_curve.py` | 对 `converted_vocal` 离线提 F0，缓存 `{stem}.f0.npz` | Phase 2 |
| `f0_build_cli.py` | 子进程生成 F0（做歌末尾可选） | Phase 2 |
| `corrector.py` | 块级：麦 PCM + 目标 F0 → 变调输出 | Phase 2 |
| `detune.py` | 跑调/人性化 LFO | Phase 2 |
| 配置 | `config/client.json` → `pitchfix.*` | Phase 1 |

### Phase 1 已完成（2026-08-08）

1. ✅ AI 跟唱按钮 + 与实时 RVC 路由分离  
2. ✅ 输出 = 伴奏 + VAD 门控 converted_vocal（按 T）  
3. ✅ 🔊 面板：伴奏 / AI人声音量 / 原唱监听 / 跟唱阈值 / 跟唱衰减  
4. ✅ 跟唱 ↔ 唱歌 handoff 共用播放头 T  
5. ✅ `scripts/test_task9_pitchfix.py` 保留 F0/修音/跑调烟测（Phase 2 模块）

### Phase 1 验收标准

1. `run_ui_skeleton.py`：选已做歌 → AI 跟唱 → 麦有声音时听到 AI 人声，停麦后按衰减释放  
2. 🔊 阈值/衰减/音量实时生效；跟唱**不**启动整段实时 RVC  
3. 哼/说/吹气均可打开门控（不验音准）

### Phase 2 未完成（条件允许再集成）

| 优先级 | 项 | 说明 |
| --- | --- | --- |
| P1 | `follow_engine=pitch_correct` | 配置开关：VAD 轨 vs 修音麦轨 vs 混合 |
| P1 | 参考 F0 改 RMVPE | 离线/缓存阶段用 RMVPE 替 librosa pyin |
| P1 | 修音引擎升级 | PyWorld/SOLA 或 rubberband 替 resample 近似变调 |
| P2 | 跑调模式 UI | 从 Phase 2 播放设置 Tab 接入 `detune.py` |
| P2 | 修音强度独立 UI | `strength` 0~100% |
| P2 | 智能模式切换 | 前奏/间奏自动切混响说话 |
| P3 | `--live` 听感验收 | 已知音阶/样例歌 AB 听感 |

### 联调备注（2026-08-07，Voicemeeter / 听感，待续）

| 现象 | 原因 / 处理 |
| --- | --- |
| VAIO 条带不动、AUX 有电平 | 客户端 `audio.output_device` 误选 **AUX Input**（如 index 24）；应选 **VAIO Input**（WASAPI 多为 index 22） |
| 关 H1→A1 后自己声音很小 | 以前靠干麦 +12 dB 监听；改听 VAIO 返回后需把 **VAIO 推子提到 0 dB**，🔊 人声约 100 |
| 回音 / 重影 | H1 同时勾 A1（干麦）+ 客户端返回；**H1 只 B1、VAIO 只 A1、VAIO 勿勾 B1** |
| 修音听感仍偏弱 / 略拖影 | **Phase 1 不修音**；Phase 2 接入 corrector 后再评估 |

### 模式切换（2026-08-07，✅ 已实现）

对标声迹：**AI 跟唱 ↔ AI 唱歌** 可随时切换，**共用播放头 T**（从当前进度继续，非从头）。

| 行为 | 实现 |
| --- | --- |
| 跟唱中点「AI 唱歌」 | 取 `PitchFollowService.position` → 停跟唱 → `WavPlayer` seek 后继续播 cover |
| 唱歌中点「AI 跟唱」 | 取 `WavPlayer.position` → 停播放 → `PitchFollowService.seek` 后启动跟唱 |
| 按钮互锁 | 跟唱/唱歌 **不再互相 disable**；仅与普通说话、托盘实时变声互斥 |
| 拖动进度条 | 跟唱模式 seek 伴奏参考轨；唱歌模式 seek WAV |
| 歌词共用 | 切换时不 stop/reload LRC；`LyricsService.sync_at` + `handoff` 保持同一条歌词会话 |
| 伴奏听感 | **AI 唱歌**播 `cover`（人声+伴奏成品）；**AI 跟唱**播 `instrumental`+可选原唱轨；已统一输出设备与时长比例 seek |

### 开发需求（Phase 2 修音，原）

1. 选歌时加载/生成参考 F0 曲线，与歌词共用播放头 `T`；
2. 可选「修音跟唱」：麦 → 修音 → 与伴奏混音；强度 0~100% 可调；
3. 「AI 唱歌」：不经修音/VAD，直播预渲染人声；
4. 延迟可控；欠载 fade；
5. UI：跟唱与说话变声（4B）入口分离；
6. 独立脚本验收，不改上游 `infer/rtrvc` 业务语义。

### 验收标准（Phase 2 修音，原）

1. `python scripts/test_task9_pitchfix.py`：F0 / 修音 / 跑调 LFO 烟测通过  
2. 可选 `--live`：唱已知音阶/样例歌，输出音高向参考靠拢可感知  
3. 跑调「关闭 vs 综合人性化」AB 听感可区分

---

# 六、打包、部署与自动更新架构

## 1. 打包方案

**首选**：Nuitka 编译打包  
**备选（推荐内测阶段）**：嵌入式 Python 目录 + 启动器 exe（与声迹 `msst-reflow/python-backend` 同类，CUDA/Torch 兼容性更好）

1. 完整内置依赖：Python 3.12、CUDA Torch、FFmpeg、RMVPE/Hubert/MSST 权重、音频底层库；
2. 输出独立单目录客户端，内置启动 exe；
3. 自动清理冗余缓存，区分开发测试包、正式发布包。

> **风险**：Nuitka + PyTorch CUDA + faiss 组合打包失败率较高，必须在任务 7 完成后单独安排 1~2 周打包专项。

## 2. 更新后端轻量化架构（Nginx / FastAPI 静态服务）

无需数据库，仅静态文件 + 简易上传接口。

### version.json 标准规范

```json
{
  "latest_version": "1.0.2",
  "force_update": false,
  "update_desc": "修复切换模型歌词错位 bug，优化低显卡推理速度",
  "full_package_url": "https://xxx/client_full_v1.0.2.zip",
  "patch_url": "https://xxx/patch_v1.0.1_to_v1.0.2.zip",
  "md_full": "文件MD5值",
  "md_patch": "补丁MD5值"
}
```

- 静态目录：版本配置、增量补丁、完整安装包；
- 日志 POST 接口：接收客户端崩溃日志、硬件信息归档存储。

## 3. 标准化发布流程

1. 迭代：单模块独立测试 → 调度层集成测试 → 打包测试包；
2. 内测：服务器上传测试包，修改 version.json 指向测试版本；
3. 正式发布：修改 latest_version，全量用户自动弹窗更新；
4. 故障回滚：version.json 指向历史稳定版本，用户重启自动回退。

---

# 七、开发迭代验收强制规范

1. 每个模块必须提供**独立启动脚本**，无需依赖未开发模块即可运行测试；
2. 同步输出单元测试代码，覆盖核心逻辑、异常场景；
3. 配套模块说明文档：入参、输出信号、依赖清单、功能边界；
4. 人工全场景测试无 bug、功能完全匹配文档，才可进入下一模块；
5. 全部模块完成后执行全链路集成测试；
6. 集成测试通过执行打包，验证安装、更新、回滚全流程。

---

# 八、后期扩展预留接口（架构提前兼容）

## Phase 2 已立项

1. **分段任务 9 Phase 2**：内置实时修音（AI 跟唱可选增强）；
2. **分段任务 8**：Enhanced LRC + 人声对齐 + 修词 UI。

## 更远期预留

1. UI 国际化多语言抽象层预留；
2. 用户配置云端同步接口预留；
3. 在线音色模型下载商城接口预留；
4. 直播歌词在线样式资源加载接口预留；
5. macOS 客户端跨平台适配抽象层预留；
6. SoundTrail `.model` 导入转换器；
7. ASIO 原生直连（绕过 Voicemeeter）；
8. 合规前提下的第三方歌词源（非加密 KRC 主路径）；
9. 改词轨音频重生成（TTS/重唱 + 再对齐）；
10. 修音算法升级（更强抗噪、和声保留、包络跟唱等）。
