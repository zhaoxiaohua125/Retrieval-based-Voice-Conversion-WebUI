# RVC AI跟唱客户端（对标SoundTrail声迹）分层开发架构需求总纲
> 文档格式：Markdown
> 技术栈：Python3.12 + PyQt6 + zhaoxiaohua125/RVC内核 + MSST(pymss) + RMVPE + sounddevice + OSC/MTC + pywin32 + Nuitka
> 运行平台：Windows 10/11
> 音频固定链路：麦克风 → Voicemeeter虚拟声卡 → 本软件RVC实时转换 → Studio One DAW混音机架
> 开发工具：Cursor3.0 分段自动化开发、单模块独立测试、迭代验收
> 交付产物：Windows独立桌面客户端 | 内置远程崩溃日志上报 + 增量自动更新系统

## 文档使用说明（给Cursor）
1. 本文档为项目全局唯一总纲，所有开发逻辑、分层规则、功能需求均以此为准，不可偏离；
2. 严格按照【分段任务1~7】顺序依次开发，单个模块独立可运行测试、验收合格后再进入下一模块；
3. 全程遵守四层分层解耦规范，禁止跨层直接调用、禁止全局变量跨线程共享状态；
4. 音频实时链路、多线程调度、更新运维功能严格遵循文档约束，保障直播低延迟与后期部署便捷性。

---

# 一、整体四层分层架构（强制全局标准）
自上而下分层，每层职责完全隔离，单模块可独立开发、单元测试，互不阻塞迭代
## 1. UI表现层（PyQt6桌面界面）
- 仅负责窗口渲染、控件交互、数据可视化展示；
- 无音频推理、IO阻塞、文件读写等耗时业务逻辑；
- 统一Qt信号槽异步通信，不使用全局变量传递状态数据。

## 2. 业务调度核心层（全局单例调度总线）
- 系统统一消息中转中心，串联UI、音频、RVC推理、歌词、更新、日志全部模块；
- 统一管控所有子线程生命周期，程序退出安全释放CUDA显存、音频流资源；
- 全局唯一时间基准分发：Studio One OSC/MTC播放头时间戳`T`；
- 统一中转异常、进度、状态信号，统一分发至UI与日志上报模块。

## 3. 底层能力内核层（可独立抽离Python库，无界面可单独测试）
拆分为5个完全独立子模块：
1. 音频IO硬件模块：声卡/Voicemeeter/ASIO枚举、环形音频缓冲、流式采集输出
2. RVC推理内核模块：实时流式转换、离线批量翻唱、模型无断音热切换、RMVPE音高提取
3. MSST人声分离内核模块：原唱人声/伴奏分离、两套业务参数预设
4. 歌词同步时钟模块：LRC解析、OSC/MTC读取DAW时间、悬浮歌词渲染、音高区间约束下发
5. 系统运维工具模块：硬件检测、分级本地日志、远程崩溃上报、客户端自动更新

## 4. 底层依赖基础层（统一环境管理）
FFmpeg、PyTorch(CUDA)、sounddevice、LoopMIDI、OSC通信库、pywin32、Nuitka打包工具

# 二、全局统一强制规范（所有模块必须遵守）
1. 所有耗时任务（AI推理、模型加载、音频分块、网络更新）全部放入独立子线程，禁止阻塞UI主线程；
2. 音频、时钟、模型、硬件状态变更仅通过**异步信号**传递，禁止跨线程全局变量共享；
3. 全局唯一时间基准：Studio One输出播放头秒级时间戳`T`，歌词渲染、人声音高约束全部依赖`T`，RVC不参与时间计算；
4. 音频隔离规则：伴奏全程不进入RVC推理模块，仅原始麦克风人声送入AI转换，规避延迟漂移、伴奏失真；
5. 用户配置持久化：声卡参数、RVC推理参数、界面布局、更新地址、热键全部存入本地JSON，开机自动加载；
6. 日志分级：`DEBUG / INFO / WARN / ERROR`；本地滚动分文件存储，程序异常自动打包日志远程上报。

# 三、四大核心业务链路完整需求
## 链路1：实时AI跟唱直播链路（最高优先级，第一阶段开发）
### 硬件信号流向
物理麦克风 → Voicemeeter硬件输入通道 → 软件音频IO线程采集人声 → RVC流式实时推理(RMVPE+音色转换) → 转换后人声回传Voicemeeter VAIO → Studio One机架EQ/压缩/混响/自动修音混音 → Voicemeeter分流耳机监听 + OBS直播推流

### 配套联动逻辑
1. 后台OSC/MTC线程持续读取Studio One播放头全局时间`T`；
2. LRC模块根据`T`实时高亮歌词，同步向RVC下发当前字标准音高区间，约束AI输出F0；
3. 多模型后台预缓存，无断音热切换：仅替换推理线程权重，音频流、伴奏、歌词线程全程不中断；
4. 全局自定义热键：启停AI、切换音色预设、伴奏播放同步；
5. 系统托盘后台常驻，窗口最小化不销毁音频与推理线程。

## 链路2：离线AI翻唱制作链路（第二阶段开发，复刻声迹「制作歌曲」）
### 业务流程
原唱音频导入 → MSST(pymss)分离（输出vocals人声 / inst伴奏 / other和声三轨） → 干人声RVC批量转换 → FFmpeg合并人声+伴奏 → 输出成品WAV/FLAC

### 配套功能
1. 两套分离预设：普通做歌、强力做歌；
2. 音频分块处理，实时进度日志，支持中途终止任务；
3. 自动识别显卡算力，算力不足自动降级CPU推理并弹窗提示；
4. 工程集合保存，批量存储多首歌曲参数，可重复加载复用。

## 链路3：歌词同步联动子系统（第三阶段开发，依赖实时音频调度）
独立后台线程运行，不占用UI、AI推理资源：
1. LRC拖拽导入、时间轴校准，结构化存储：`[起始秒数, 结束秒数, 歌词文本, F0最小值, F0最大值]`；
2. 双同步方案：优先OSCalot OSC协议，备选LoopMIDI MTC MIDI时间码；
3. 独立Qt悬浮歌词窗口：无边框、置顶、透明、可拖拽、自定义字体/颜色/大小，适配OBS采集；
4. 实时下发当前歌词音高区间至RVC，切换音色后人声与文字无错位；
5. 歌词配置本地缓存，重启自动加载。

## 链路4：软件运维子系统（第四阶段开发，发布部署必备）
### 4.1 远程Bug/异常上报
1. 全局捕获程序崩溃、CUDA报错、声卡断开、IO异常，自动抓取完整堆栈；
2. 本地日志自动打包，HTTP POST上传远程日志服务；
3. 支持用户手动一键导出日志压缩包线下排错；
4. 上报携带环境信息：软件版本、Windows版本、显卡型号、CUDA算力、音频设备参数。

### 4.2 客户端自动更新
1. 启动后台线程请求远程`version.json`版本接口，对比本地版本；
2. 双更新策略：小版本增量补丁、大版本完整客户端包；
3. MD5文件完整性校验，下载失败自动重试；更新完成弹窗提示重启；
4. 更新失败自动回滚旧版本，保证客户端不损坏；
5. 远程可配置：更新地址、强制更新开关、版本公告文本。

# 四、分模块开发任务清单（Cursor分段执行，独立测试验收）
## 分段任务1：PyQt6 UI表现层（无音频依赖，优先开发）
### 开发需求
1. 主窗口对标SoundTrail布局：
    - 左侧：文件资源栏（伴奏/人声/LRC/模型管理）
    - 中部：标签页（实时跟唱、离线制作、模型管理、系统设置）
    - 右侧：参数面板（RVC高级参数、MSST参数、音频设备下拉框）
    - 底部：实时日志窗口、GPU硬件监控状态栏
2. 独立悬浮歌词子窗口：无边框、置顶、透明度、拖拽、样式自定义；
3. 系统托盘完整菜单：启停AI、打开主界面、退出、手动检查更新；
4. 设置页面：音频硬件、OSC/MTC参数、更新地址、日志路径、全局热键；
5. 模型管理面板：批量导入`.pth/.model`、删除、分组、后台预缓存；
6. UI全部通过信号槽与调度层解耦，界面无业务计算、音频逻辑；
7. 窗口布局自动保存JSON，开机还原。

### 验收标准
单独启动脚本完整渲染界面，所有控件交互正常；悬浮窗口、托盘功能可用；未接入音频仅友好提示，无崩溃报错。

## 分段任务2：系统运维工具模块（无音频依赖，独立单元测试）
### 开发需求
1. 分级日志类：自动滚动分割、单文件大小限制、自定义存储路径；
2. 全局异常捕获装饰器：捕获Python、CUDA、音频硬件异常；
3. 远程日志上报HTTP客户端，自动携带设备环境信息；
4. 自动更新工具类：版本请求、MD5校验、增量下载、更新替换、版本回滚；
5. 硬件采集工具：读取Windows、显卡、CUDA、全量音频设备列表；
6. JSON配置读写工具，持久化全部用户参数。

### 验收标准
独立脚本可测试日志写入、硬件读取、版本校验、模拟异常自动打包日志；更新下载、校验、回滚流程完整。

## 分段任务3：音频硬件IO调度模块（依赖工具模块，独立声卡测试）
### 开发需求
1. 封装sounddevice，枚举WDM/ASIO设备，自动识别Voicemeeter/VB-Cable虚拟声卡并标记；
2. 音频流管理类：创建/启停/销毁，内置50ms环形缓冲池；
3. 全局音频参数统一：48000Hz、24bit，缓冲块可调；
4. Voicemeeter通道专用识别逻辑，区分硬件输入/VAIO人声/AUX伴奏；
5. 音频容错：热插拔自动重连、爆音平滑、自动关闭独占音频模式；
6. 独立子线程采集音频，异步信号推送音频块至调度层。

### 验收标准
独立脚本可枚举全部声卡；稳定创建输入输出流，低延迟无持续爆音；插拔声卡正常捕获异常提示。

## 分段任务4：RVC推理内核模块（依赖音频模块，分实时/离线）
基于zhaoxiaohua125/RVC-WebUI剥离Gradio，封装纯Python调用类：
### 开发需求
1. 离线批量推理类：接收MSST干人声，分块转换，对接FFmpeg合并伴奏；
2. 实时流式推理类（改造rtrvc）：
    - 多模型后台线程预缓存；
    - 环形缓冲平滑切换，无断音热切换；
    - RMVPE F0、HuBERT特征、RVC v2完整管线；
    - 支持外部传入歌词音高区间约束输出；
3. 硬件自动调度：检测CUDA，无N卡切CPU，低算力显卡弹窗提示；
4. 参数完整映射UI：音调偏移、Index比例、最小/最大F0、说话人ID；
5. 推理进度、GPU占用异步推送UI日志。

### 验收标准
独立脚本完成离线人声转换；实时流稳定输出AI人声；切换音色无断音；GPU/CPU自动切换正常。

## 分段任务5：MSST人声分离内核模块（依赖FFmpeg、推理模块，离线专用）
### 开发需求
1. 封装pymss/MSST底层，实现「普通做歌/强力做歌」两套预设；
2. FFmpeg解码全格式音频，输出标准三轨WAV：vocals / inst / other；
3. 大音频分块处理，限制内存，实时推送进度信号；
4. 支持中途终止，自动清理临时缓存；
5. 分离完成推送文件路径信号至调度层。

### 验收标准
导入原唱自动拆分三轨；两套模式效果区分明显；中断任务无残留缓存文件。

## 分段任务6：歌词同步&Studio One时间同步模块（依赖音频调度）
### 开发需求
1. LRC解析工具，生成带音高区间结构化数据，支持时间校准；
2. OSC客户端高频读取Studio One OSCalot播放头时间`T`；
3. 备选MTC MIDI同步方案（LoopMIDI虚拟端口）；
4. 传输延迟补偿逻辑，消除OSC微小偏移；
5. 根据`T`匹配歌词条目，向外推送文字+音高区间信号；
6. 实时下发音高参数至RVC推理线程约束F0。

### 验收标准
连接Studio One实时同步播放进度；LRC精准匹配时间；音高参数可正常生效。

## 分段任务7：中央调度核心层（最后集成，串联全部模块）
### 开发需求
1. 全局单例调度器，统一管理所有子线程生命周期；
2. 中转全模块异步信号，统一管理全局状态（AI启停、播放、模型切换、更新）；
3. 全局时间`T`统一分发至歌词、RVC模块；
4. 全局异常统一中转，自动触发日志上报；
5. 音频链路联动：启动跟唱自动初始化音频+推理线程，关闭有序释放资源；
6. 模块完全解耦，调度层仅消息转发，不实现业务逻辑。

### 验收标准
所有模块接入后，实时跟唱、离线翻唱、歌词同步全链路打通；软件启停无内存泄漏、无CUDA显存残留。

# 五、打包、部署与自动更新架构
## 1. 打包方案：Nuitka编译打包（替代PyInstaller）
1. 完整内置依赖：Python3.12、CUDA Torch、FFmpeg、RMVPE/Hubert/MSST权重、音频底层库；
2. 输出独立单目录客户端，内置启动exe；
3. 自动清理冗余缓存，区分开发测试包、正式发布包。

## 2. 更新后端轻量化架构（Nginx / FastAPI 静态服务）
无需数据库，仅静态文件+简易上传接口
### version.json 标准规范
```json
{
  "latest_version": "1.0.2",
  "force_update": false,
  "update_desc": "修复切换模型歌词错位bug，优化低显卡推理速度",
  "full_package_url": "https://xxx/client_full_v1.0.2.zip",
  "patch_url": "https://xxx/patch_v1.0.1_to_v1.0.2.zip",
  "md_full": "文件MD5值",
  "md_patch": "补丁MD5值"
}
```
- 静态目录：版本配置、增量补丁、完整安装包；
- 日志POST接口：接收客户端崩溃日志、硬件信息归档存储。

## 3. 标准化发布流程
1. 迭代：单模块独立测试 → 调度层集成测试 → 打包测试包；
2. 内测：服务器上传测试包，修改version.json指向测试版本；
3. 正式发布：修改latest_version，全量用户自动弹窗更新；
4. 故障回滚：version.json指向历史稳定版本，用户重启自动回退。

# 六、开发迭代验收强制规范
1. 每个模块必须提供**独立启动脚本**，无需依赖未开发模块即可运行测试；
2. 同步输出单元测试代码，覆盖核心逻辑、异常场景；
3. 配套模块说明文档：入参、输出信号、依赖清单、功能边界；
4. 人工全场景测试无bug、功能完全匹配文档，才可进入下一模块；
5. 全部模块完成后执行全链路集成测试；
6. 集成测试通过执行Nuitka完整打包，验证安装、更新、回滚全流程。

# 七、后期扩展预留接口（架构提前兼容）
1. UI国际化多语言抽象层预留；
2. 用户配置云端同步接口预留；
3. 在线音色模型下载商城接口预留；
4. 直播歌词在线样式资源加载接口预留；
5. MacOS客户端跨平台适配抽象层预留。